# Claude Guidance

Start with `AGENTS.md`, then read `CUSTOM_AGENT_GUIDANCE.md`.

This repository is the minimal starter template used by `apiease-cli` for new APIEase projects. Prefer small, template-friendly changes centered on `apiease.config.js` and the `resources/` directories. Do not add unrelated framework structure unless the user explicitly asks for it.
