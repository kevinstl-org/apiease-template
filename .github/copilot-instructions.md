# Copilot Instructions

Read `AGENTS.md` and `CUSTOM_AGENT_GUIDANCE.md` before making changes.

This repository is a minimal APIEase template, not a prebuilt application. Keep changes focused on `apiease.config.js` and `resources/`. Avoid adding build systems, frameworks, or heavy scaffolding unless the user explicitly requests them.
